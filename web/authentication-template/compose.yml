services:
  fruggr-scanner:
    image: fruggr/scanner:latest
    pull_policy: always
    environment:
      - FRUGGR_API_KEY
      - FRUGGR_AUTH_STEPS
    volumes:
      - "./work/script.json:/tmp/fruggr/script.json"
